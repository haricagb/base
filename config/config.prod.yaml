# config/config.prod.yaml — Production overrides

jwt:
  secret: ""  # MUST be set via APP_JWT_SECRET env var
  access_token_expiry: 15m
  refresh_token_expiry: 168h  # 7 days
  issuer: "my-application"
